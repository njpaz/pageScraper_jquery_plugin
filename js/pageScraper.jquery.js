(function ($) {
  $.fn.pageScraper = function(options) {
  	var defaults = {
      element: '#link_holder'
    },
    settings = $.extend({}, defaults, options)

    this.each(function() {
    	
    })
  }
}(jQuery))